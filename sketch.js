function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  circle(mouseX, mouseY, 20);

}
function draw() {
function draw() {
  background(255);
  fill(mouseX);
  strokeWeight(mouse)
  textSize(20);
  text(mouseX, 100, 100);

}
